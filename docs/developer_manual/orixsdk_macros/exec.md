#exec
	;----------------------------------------------------------------------
	;
	; usage:
	;	exec command
	;
	; note:
	;	command may be : (ptr), address
	;
	; Call XEXEC function
	;----------------------------------------------------------------------
	exec command